[project]
name = "open-circuits"
version = "0.1.0"
description = "Build tooling for Open Circuits — A Portable Electronics Reference"
requires-python = ">=3.9"

[project.optional-dependencies]
dev = ["pytest>=7.4"]
